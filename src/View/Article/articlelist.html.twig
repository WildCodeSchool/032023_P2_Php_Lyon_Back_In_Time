{% extends 'layout.html.twig' %}


{% block content %}
    <h1>Nos articles</h1>
    <ul>
        {% for article in articles %}
            <li><a href="/articles/show?id={{ article.id }}">{{ article.id }}. {{ article.title }}</a></li>
        {% else %}
            <li>Nothing to display</li>
        {% endfor %}
    </ul>
{% endblock %}